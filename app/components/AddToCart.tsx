'use client';
function AddToCart() {
    return (
        <div>
            <button onClick={() => console.log('add to card')}>
                Add to card
            </button>
        </div>
    );
}

export default AddToCart;
